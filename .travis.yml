language: "perl"
perl:
  - "5.18"
  #- "5.14"
  #- "5.12"
  #- "5.8"

before_install:
    - cpanm IPC::ConcurrencyLimit --notest
    - cpanm Perl::Critic --notest
# - cpanm -n Devel::Cover::Report::Coveralls
# see http://search.cpan.org/~mikihoshi/Devel-Cover-Report-Coveralls-0.06/lib/Devel/Cover/Report/Coveralls.pm

# should really use dependency manager and --install-deps as below

install:
    - echo 1 # we don't actually install anything yet
#    - cpanm -v --installdeps --notest --mirror http://cpan.mirrors.travis-ci.org .

#script: "perl Build.PL; ./Build test"

script:
# - perl -c src/*.pl # no perl scripts currently
  - perl -c src/*.pm
  - perlcritic --verbose 5 t/
  - perlcritic --verbose 5 src/
# - perlcritic --brutal --verbose 5 t/
# - perlcritic --brutal --verbose 5 src/
  - prove

#branches:
#   only:
#     - master

notifications:
  email:
    on_success: change
    on_failure: always

#env:
